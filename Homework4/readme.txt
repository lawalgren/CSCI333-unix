getBand downloads a website I built a few years ago and replaces the band's name in each html file with "MyBand"

pull_my_repos runs git pull on each repo in my projects directory and formats the output, stopping on the first error.
